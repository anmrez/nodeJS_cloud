<section class="block">

  <section class="container">
    <section class="">
      {{#if errors}}
      <section class="df_ja err">
        <span class="df_ja"> <p> ! </p> </span>
        <p>Error: user not found </p>
      </section>
      {{/if}}
      {{#if cookieName}}
      <section>
        <h1>Вы уже залогинились</h1>
      </section>
      {{else}}
      <form class="form_names" method="post">
        <section class="input">
          <input name="userName" type="text" required autocomplete="off" value="" />
          <label class="label__input" for="name" >
            <span>
              Name
            </span>
          </label>
        </section>
        <section class="container__form__submit">
          <input class="form__submit" type="submit" value="login"/>
        </section>
      </form>
    </section>

    {{#if errors}}
    <section class="register df_ja">
      not <a href="/registration"> registered?</a>
    </section>
    {{/if}}
    {{/if}}




  </section>
</section>
